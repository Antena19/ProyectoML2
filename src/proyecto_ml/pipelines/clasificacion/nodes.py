#Paso 3: Creamos el nodo para cargar el dataset
import pandas as pd

def cargar_datos(df: pd.DataFrame) -> pd.DataFrame:
    """Retorna el dataset sin modificar."""
    return df
"""
Nodos de Clasificación.

Objetivo:
- Cargar el dataset de nacimientos sin transformaciones para usos posteriores.

Salida:
- El mismo DataFrame recibido (`datos_cargados`).
"""
